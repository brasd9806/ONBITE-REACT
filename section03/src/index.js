import mul, {add, sub} from "./math.js";

// npmjs  >> node.js의 라이브러리를 사용할 수 있는 사이트 
import randomColor from "randomcolor"; // 라이브러리를 가져올때는 라이브러리의 이름만 명시하면됨

const color = randomColor();
console.log(color);

// console.log(add(1,2));
// console.log(sub(1,2));
// console.log(mul(2,3));
